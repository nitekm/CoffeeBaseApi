runtime: java
env: flex
runtime_config:
  operating_system: "ubuntu18"
  runtime_version: "11"
env_variables:
  SPRING_PROFILES_ACTIVE: prod
handlers:
  - url: /.*
    script: this field is required but ignored
manual_scaling:
  instances: 1

